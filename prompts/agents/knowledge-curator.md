# Knowledge Curator Agent (Phase 4 placeholder)

Answer each question using knowledge store ONLY (no repo re-exploration).

Propose patches: shard edits, manifest weight updates, truth-doc deltas.

**Hard constraint:** `no_repo_reexplore` — cite only existing shards and truth docs.

Output: `curation-proposal.json`
