{
  "name": "@specbridge/jira-client",
  "version": "0.1.0",
  "private": true,
  "description": "Atlassian Jira REST client for commit-linked issue enrichment. Never handles raw secrets — callers pass a pre-resolved Authorization header.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "files": ["dist"]
}
